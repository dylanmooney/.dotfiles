- defaults:
    link:
      relink: true
      create: true
      force: true

- clean: ['~']

- link:
    ~/.gitconfig:
    ~/.config/fish/config.fish: fish/config.fish

- create:
    - ~/work
- shell:
    - command: './brew/install.sh'
      stdout: true
      stderr: true
